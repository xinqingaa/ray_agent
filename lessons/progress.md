# 课程制作进度

项目阶段与学习验收见 [PLAN](../PLAN.md)，课程问题与顺序见[目录](README.md)，研究入口见[素材索引](map.md)。本文件维护当前制作状态与必要验证证据，不记录逐次编辑过程。

## 当前重点

第 8–11 章已按同一节奏改写：问题 → Harness 策略 → RayAgent 案例 → 收回判断。第 11 章补了开篇总表和服务指南观察入口；既有夹具与脚本未在本轮重跑。第 12 章已按 Harness 文件组织与成果交付主线重写，待读者审阅；第 13–17 章仍为骨架。第 08–11 章正常路径不覆盖完整断连恢复、API 崩溃、TTL 到期删除、租户隔离与多执行者接管；故障实测由第 16 章承接。

## 章节状态

| 章节 | 正文状态 | 验证范围与缺口 | 下一步 |
|---|---|---|---|
| [01 · 认识 Agent Harness：从一句请求到任务完成](01-the-rayagent-system.md) | 任务叙述与职责解释补充完成，待审阅 | 职责静态核对；1 张 SVG 渲染检查通过；本章产品未复跑 | 审阅职责层次，并与第 06 章已取得的共同任务证据对照 |
| [02 · 与模型交互](02-model-interaction.md) | 复现条件、用量含义与简化 Python 补充完成，待审阅 | 6 项模型交互及配置测试通过；保留原 SVG；真实模型调用 unverified | 固定并记录请求条件，验证实际分块、结束原因和可得用量 |
| [03 · 工具与行动](03-tools-and-actions.md) | 工具说明取舍、校验与授权边界补充完成，待审阅 | 4 项本地夹具测试通过，包含 `3_8` 解析脚本；修改的 SVG 通过 Fireworks 和 Chrome 目视检查；真实模型调用 unverified | 用保留旧日志内容的任务核对工具模式选择，区分描述对照与执行契约验证 |
| [04 · Agent Loop 与 ReAct](04-agent-loop-and-react.md) | 控制流改为简化 Python，待审阅 | 4 项本地夹具测试通过；原 SVG 保留；真实模型调用 unverified | 配置模型后验证 `4_1` 是否连续提出写入和读取 |
| [05 · 上下文与记忆](05-context-and-memory.md) | 压缩检查、按需加载与清理条件补充完成，待审阅 | 5 项请求工作集测试通过；压缩条件静态核对；3 张原 SVG 保留；真实 token 与 `4_2` 分词 unverified | 用资料 A 同一判断问题比较条件保留和原文重取，分别记录材料核对与模型表现 |
| [06 · 从 Agent Loop 到完整 Harness](06-from-agent-loop-to-harness.md) | 草稿完成，待审阅 | 真实 Web 文件任务通过，详见下方记录；2 张 SVG、2 张截图已核对；未捕获完整模型请求 | 审阅实际交接讲解与图文密度；完整出站请求正文未捕获 |
| [07 · 规划与内外层循环](07-planning-and-nested-loops.md) | 计划合并边界补充完成，待审阅 | 4 项真实流程夹具通过，覆盖第二步继续、收缩、`success=false` 与失败中止；两张原 SVG 保留；新增真实模型运行 unverified | 对照确定性事件轨迹审阅正文；另行观察真实模型的计划调整是否合理 |
| [08 · 任务执行与控制](08-task-execution-and-control.md) | 各节按问题 → 策略 → 实现收束；压缩源码路径与跨会话等待旁证 | 既有控制用例、Shell 进程观察与 Docker 正常/等待/停止证据仍适用；3 张原 SVG 未改。重复提交、SSE 断连、停止长 Shell 进程及动态沙箱回收仍 `unverified` | 审阅分节先后是否清楚；第十六章设计重复提交、断连和进程取消故障实验 |
| [09 · 状态与持久化](09-state-and-persistence.md) | 总表改为停住之后要分清什么；等待续接补重新组装这一拍；不再把「三类寿命」输出为专名 | 既有状态夹具与同会话等待续接证据仍适用；3 张原 SVG 未改。API 崩溃、事务故障、执行位置替换、多执行者接管与 SSE 故障补齐仍 `unverified` | 审阅总表与等待续接是否好读；故障实测承接第 16 章 |
| [10 · 事件与可观察性](10-events-and-streaming.md) | 开篇接第 9 章结论；关联节改为先讲通用追踪再对照当前字段；事件清单补 `title` 与 `error` | 既有事件用例、用量用例、UI 解析观察与第九章 32 条事件复核仍适用；2 张原 SVG 未改。完整 SSE 故障、模型全尝试账本与跨服务追踪仍 `unverified` | 审阅关联节先后；第十六章承接断连与解析故障实验 |
| [11 · 沙箱与执行环境](11-sandbox-and-execution-environment.md) | 正文按总分总补执行环境地图；「容器 ≠ 隔离」拆成文件系统、网络、身份、配额四轴并各补通用对照；补观察判断表与判断清单；按实测修正 TTL 与已有沙箱销毁表述 | 身份（uid=0）与 TTL 计时状态（`active=true`）已由产品路径只读实测补证；本地边界脚本与 Docker 容器观察未重跑。自动到期的完整回收、应用关闭清理、公网出口与租户隔离仍 `unverified` | 审阅四轴表述与 TTL「已启用但用沙箱默认值」的写法；确定是否修改系统提示中的 Node 版本与执行身份；第十六章承接到期回收与关闭清理 |
| [12 · 文件与任务产物](12-files-and-artifacts.md) | 按 Harness 主线重写完成，待审阅：文件用途、输入准备、工具反馈、成果选择、交付策略与验证；替换为一张通用职责图 | 本轮静态核对代码依据、链接与章节分工，新 SVG 结构与 Chrome 渲染目视检查通过；既有 91 项测试及数据库观察未重跑，开篇分析任务仅作教学推演 | 审阅工程职责是否清楚、案例是否适量；第十六章承接故障实测，跨会话工作区由第十七章展开 |
| [13 · 浏览器如何成为工具](13-browser-as-a-tool.md) | 骨架 | 未进行本章运行验证 | 核对页面、截图与文本的时效；用本地页面推演不可信指令进入 Context 的路径及重新观察条件 |
| [14 · 通过 MCP 接入外部工具](14-external-tools-with-mcp.md) | 骨架 | 未进行本章运行验证 | 按现有协议基线核对发现、调用、认证、超时与取消；明确工具说明、结果与副作用的信任边界 |
| [15 · 通过 A2A 协作远程 Agent](15-agent-collaboration-with-a2a.md) | 骨架 | 未进行本章运行验证 | 制作远程终态、本地结果和取消传播对照；区分结果验收与协议调用，说明重复提交及多 Agent 调度的能力边界 |
| [16 · 可靠性、验证与评估](16-reliability-and-verification.md) | 骨架 | 现有夹具只提供局部证据；未进行本章任务集或故障实验 | 定义五类任务的环境、目标、评分和失败分类；固定条件重复比较一次改动，并核对评分器、质量、耗时与用量 |
| [17 · 长任务与项目工作区](17-from-sessions-to-workspaces.md) | 骨架 | 待核对架构现状 | 以暂停交接同一项目组织目录、产物版本、待办与证据；比较并发修改归属和隔离方案，标明演进建议 |

## 验证依据

以下区分已有运行记录与各章静态核对；正文完成不表示新增运行验证。

### 第 02–05 章本地实验

2026-09-12 在 foundations 的锁定环境（Python 3.12.10 / Requests 2.32.5）重跑全部 19 项本地测试通过，无需外部模型：

- 第 02 章的 6 项测试包含配置地址拼接，以及同输入对照、首段提前显示、空增量与统计块、length 结束、缺失终止标记、HTTP 错误及缺少模型配置。
- 第 03 章覆盖参数校验、无工具时不执行、工具结果回传与 `tool_choice="none"`，以及强制工具选择提取字段；`3_8` 解析脚本已实际运行。
- 第 04 章覆盖纯文本停止、工具反馈后继续请求、迭代超限与 `3_7` 第二次调用锁死工具。
- 第 05 章验证示意消息与输出，不证明真实模型行为、残缺请求的协议有效性或服务端 token 数。`4_2` 两处正文不同，且缺少完整词表，不能把长度差归因于模板。

第 01–06 章 SVG 已做结构与 Chromium 渲染目视检查；使用 Chromium 的原因是通用脚本所需 CairoSVG / rsvg-convert 不可用。临时预览未提交。上述本地验证不包含第 02–05 章真实模型调用。

本轮仅修改 `03-tool-call-execution.svg`，保留原接收与回传结构，加入执行前的调用检查，并明确通用职责不代表已有审批能力。1600×900 SVG 通过 Fireworks 1.2.0 的 XML、marker、碰撞、几何与构图检查，以及 Chrome 实际渲染目视检查。第 02、05、07 章配图未改；未使用的 `01-nested-loops.svg` 经引用检查后删除。第 02–05、07 章 Python 示例通过语法解析，简化模型不作为独立脚本验收。

### 第 06 章产品任务

2026-09-11，本机 Compose 的 API、UI、PostgreSQL、Redis 健康。Web 新建会话 `e9e13df2-b049-4c94-a6b2-8494b5555653`，执行正文中的文件任务；日志模型标识为 `qwen3.8-27b`。终态 `completed`，保存 21 条事件并含 `done`。初始计划 3 步，实际仅步骤 1 运行并完成，更新后保留 1 个已完成步骤。模型工具调用依次为通知、写入、通知、读取、通知，未调用 MCP/A2A 或浏览器工具。

最终附件 ID 为 `439a71cc-dd36-4394-8053-5daa4eb0f1b4`，存储 key 为 `2026/09/11/439a71cc-dd36-4394-8053-5daa4eb0f1b4.txt`。动态沙箱 `rayagent-sandbox-f3f53516` 的 `/home/ubuntu/hello.txt`、API `/data/files/` 下对应文件，以及 `/api/files/{file_id}/download` 响应，均为 `b'hello'`、5 字节。

Web 已打开工具记录与最终附件，刷新后历史和附件入口保留。两张截图来自同一会话，隐藏无关任务侧栏；历史工具标签仍显示“正在读取”，正文已说明其不代表运行状态。完整出站模型请求未捕获，Context 表依据源码解释。该成功任务不覆盖故障场景。

维护时更新对应章节的状态、验证范围和一个具体下一步；仅在复核结论需要时补充详细证据，已完成的编辑过程交由 Git 历史保留。

### 第 07 章实现与配图

2026-09-11 静态核对 `PlannerReActFlow.invoke`、`PlannerAgent.create_plan/update_plan`、`ReActAgent.execute_step/summarize`、`BaseAgent.invoke`、计划模型与对应提示。重点核对已结束步骤前缀的保留、无未结束步骤时不合并新列表、`success` 与步骤状态分离，以及 `FAILED` 步骤不进入更新分支。复用第 06 章已有任务证据；另一个两步骤案例为教学示意，未新增真实模型、产品或失败路径运行。

`07-nested-feedback.svg` 已重绘为上下两个独立流程视图（1600×900、Style 1），通过 XML、marker、碰撞、几何与构图检查，并以 Chrome headless 渲染目视检查。通用渲染器不可用，浏览器代替其渲染环节；临时 PNG 不提交。

2026-09-12，对比小节保留“双循环编排与单循环”主线，修正执行器被步骤硬性限制、单循环全由模型控制等过强表述。核对官方 App Server 的计划事件、取消与审批，接口资料未用于推断全部内部循环。`07-progress-control.svg` 改为左右对照：左侧外层交接包住工具反馈，右侧计划工具与其他工具共用反馈循环；图注已同步。使用 Fireworks Tech Graph 1.2.0，XML、marker、碰撞、几何、构图和 Chrome 渲染目视检查通过。临时 PNG 不提交，未新增产品或模型运行。

2026-09-12 在 API 锁定环境（Python 3.12.10 / pytest 8.4.2）运行 `tests/core/test_planner_react_flow.py`，4 项通过。保留实际 Flow、Planner、ReAct、工具分发和内层调用；模型响应、JSON 解析适配、沙箱与存储使用本地替身。核对第一步工具观察进入模型、步骤结果进入 Planner、第二步继续或被移除、最后一步后的新待办不合并、`success=false` 仍清理并更新，以及解析失败在清理、更新和总结前中止。事件按交接时刻拷贝，避免最终可变对象掩盖中间状态。

同轮运行 `tests/core/test_llm_usage.py` 的 7 项测试通过，核对用量解析、缺失值、累计与事件映射；不能替代服务端真实 token 观测。运行命令归 [API 指南](../ray_agent/api/README.md#测试与数据库迁移)。本轮没有连接模型、数据库、Redis 或产品沙箱，未新增真实模型、产品端到端或故障注入运行。


### 第 08 章执行控制与配图

2026-09-12，基于代码基线 `dd58aff` 核对应用协调、任务注册、运行器、等待触发与回复配对、内层循环及 Shell 服务。教材区分询问与动作审批、实例内防重入与请求去重、取消请求与实际停止、请求超时与总预算。本轮未修改产品控制逻辑。

在 API 锁定环境（Python 3.12.10 / pytest 8.4.2）运行第八章定向入口，共 14 项通过：新增 `test_task_execution_control.py` 的 8 项，既有取消辅助用例 2 项与双循环用例 4 项。保留实际协调、运行器、任务适配或 Flow/Planner/ReAct，按用例替换模型、消息传输、仓库、沙箱和附件处理。等待用例用新运行器加载保存的计划及回复，未重新创建初始计划；补充输入用例在旧读取的 calling 事件发布时注入，旧工具未执行，重新规划后读取新路径。取消用例阻塞协议清理，确认接口返回、注册移除和终态记录可先于执行协程结束。迭代用例核对上限为 1 时，即使工具后的模型回复已收口，也会先产生超限错误；同序列上限为 2 则正常停止。夹具不连接真实 Redis、数据库或模型，不证明多进程并发排他与外部清理成功。

同日在沙箱锁定环境（macOS / Python 3.10.17）运行 `scripts/check_shell_control.py`，直接使用实际 ShellService 与单个系统进程。启动后约 1.03 秒等待超时，进程仍存活；约 1.093 秒取消调用协程后，文件从 50 字节增长为 53 字节；显式终止后退出码为 -15，文件停止增长且已有内容仍存在。脚本最终确认进程退出并删除临时目录。实验使用 exec 替换外层 shell，不覆盖进程树、忽略信号或跨服务取消传播。命令分别维护在 API 和沙箱指南。

第八章本地夹具制作时 Docker socket 不可用；同日后续真实 Docker 运行已取得正常任务、等待与规划阶段停止证据。正常文件任务为 `b640bdd3-4e18-442b-a145-a23a7f70ce21`；等待会话 `c0afd874-f6d5-4db8-b7dc-7e56e4c817ae` 回复后尝试读取前者创建的 `/home/ubuntu/ch08-normal.txt`，因当前会话沙箱中不存在该文件而再次等待。该跨会话观察不能证明同会话等待丢失文件。停止会话 `6a22b758-e07e-493a-b821-d7b3b7dbc06d` 在规划阶段停止，未进入长 Shell 调用；重复提交、SSE 断连、长进程取消和动态沙箱回收仍 `unverified`。API 替身测试与 Shell 本地进程观察仍是独立证据，不能合称一次端到端验证。

新增 `08-task-handoff.svg`、`08-wait-and-resume.svg`、`08-cancel-boundaries.svg`，均为 1600×900、Fireworks Style 1。使用已安装的 Fireworks Tech Graph 1.2.0，通过 XML、marker、碰撞、几何、构图检查，并经 Chrome headless 实际渲染目视检查；临时 PNG 放在工作区之外，不提交。正文链接、Python 简化片段语法及图文对应关系已核对。


### 第 09 章状态与持久化

2026-09-12–13，基于 `e22b1e5` 核对状态模型、ORM 转换、仓库与 UoW、BaseAgent、ReAct、Flow、任务运行器、Redis 任务和队列、沙箱查找及前端历史投影。已完整阅读第 01–08 章，将本章衔接到第五章的信息分层、第六章的记录与交付、第七章的步骤粒度和第八章的等待退出。未修改产品执行或存储逻辑。

2026-09-12 在 API 锁定环境（Python 3.12.10 / pytest 8.4.2）运行 `test_state_persistence.py`、`test_task_execution_control.py` 和 `test_planner_react_flow.py`，共 16 项通过，其中新增 4 项。状态用例保留实际 Agent / Flow 或事件发布方法，固定模型并替换存储、传输和沙箱：

- 临时文件写入后在 `called` 交接处序列化会话，确认 Memory 尚无工具结果；继续消费后结果进入 Memory，停止消费后新 Agent 的消息回滚不撤销文件。两种路径分别参数化验证。
- 输出队列发布后令仓库保存失败，确认输出已存在、历史未新增；不模拟 PostgreSQL 的提交或崩溃行为。
- 正常等待后把会话序列化并重建，再用新运行器续接。最新 PlanEvent 的步骤为 `pending`，StepEvent 已为 `running`；提问回复正确配对，没有重新创建初始计划，原会话对象保持等待。此测试不依赖跨运行器共享可变对象，也不模拟进程重启。

2026-09-12 22:14–22:16（Asia/Shanghai），通过聊天 API 完成真实同会话实验 `f1975a49-b29a-47d1-8e3c-234914f41045`。任务要求写 `/home/ubuntu/ch09-resume.txt` 为 `hello-ch09` 后调用 `message_ask_user` 等待，回复后读取并交付。等待时有 15 条事件、状态 `waiting`，ReAct Memory 为 7 条且末尾是提问调用。回复后事件增至 32 条、状态 `completed` 并含 `done`；ReAct Memory 为 16 条。任务 ID 从 `05308430-7484-4eed-bd0c-c12520d50282` 变为 `93947696-e45d-462c-b202-3a8f3431e36d`，沙箱 ID 均为 `rayagent-sandbox-c2ff0e62`。工具记录中只有一次写入和一次读取，此外有两次通知；calling / called 分别计阶段，不计为重复调用。初始三步计划在续接完成后收缩为一个已完成步骤。

等待和完成时直接读取同一沙箱文件，均为 `b'hello-ch09'`、10 字节。最终附件 ID 为 `ef168841-8ac7-452a-87f7-4a7f4d26e414`，key 为 `2026/09/12/ef168841-8ac7-452a-87f7-4a7f4d26e414.txt`，下载字节一致。会话另保存两份同路径同步记录，本章不据此推断版本去重或持续同步。已在浏览器新打开并刷新该会话，确认询问、回复、工具记录和最终附件保留。等待与回复由 API 发起，页面只验证历史加载和刷新，不称为页面提交全流程测试。

运行容器中的 BaseAgent、ReAct、Flow、运行器、AgentService 和会话仓库与本地文件逐字节一致。未捕获完整出站模型请求，Memory 快照不能替代实际请求转录；运行不覆盖 API 崩溃、数据库事务故障、Redis 重启、沙箱替换、SSE 故障补齐或多执行者接管。真实记录和临时快照在本机观察，文档仅保留复核所需标识与结论。

2026-09-13 完成 `09-state-ownership.svg` 与 `09-action-recording.svg`（1600×900、Fireworks Style 1）。XML、marker、碰撞、几何与构图检查通过；通用 PNG 渲染器不可用，改用 Chrome headless 实际渲染并目视检查。临时预览未加入仓库。正文 Python 简化片段语法、相关链接和图文对应关系已检查；前八章仅修正第八章直接相关的跨会话证据表述。


### 第 10 章事件与可观察性

2026-09-13，基于 `ae9803a` 核对领域事件、接口映射、模型适配、BaseAgent 重试、运行器累计、日志与配置路径，以及 UI 的流解析、详情 hook 和时间线投影。正文衔接第二章的响应接收、第六章的工具预览、第八章的执行控制与第九章的保存间隔；未修改产品执行、事件契约或解析器。

在 API 锁定环境（Python 3.12.10 / pytest 8.4.2）运行 `test_event_observability.py`、`test_llm_usage.py` 和 `test_state_persistence.py`，16 项通过，其中新增事件用例 5 项。覆盖领域事件 JSON 往返后的实时/历史映射一致、工具原结果与计划身份等字段的省略、亚秒时间取整；两次空回复重试耗尽时未交出暂存 usage，而最终有效回复会交出两次 usage；一对工具事件可以包含两次底层执行尝试。出现既有 `json_encoders` 弃用警告，不影响本次断言。夹具固定模型并替换存储与沙箱，不验证外部服务计费或真实数据库故障。

在 UI 安装锁定依赖后，用 Node.js 24.12.0 运行 `scripts/check-event-observability.cjs`，加载实际 TypeScript 模块。三项正常观察通过：LF 逐字节（含中文 UTF-8）解析与整块一致；非法 JSON 进入错误回调；按调用标识合并阶段并在新用户消息后重新分组步骤。两项限制得到复现并标记 `LIMITATION`：EOF 尝试分派无空行结尾的完整 JSON；`event: tool\r` 与后续 `\ndata: ...` 分块时，当前行尾处理会丢失工具事件类型。退出成功只表示现有行为与限制被复现，不表示 SSE 规范符合性通过。未挂载 React hooks、启动浏览器故障实验或运行完整构建；开发与容器基线仍为 Node.js 22。

本次复核第九章 2026-09-12 的真实会话 `f1975a49-b29a-47d1-8e3c-234914f41045`：此前保存的两轮 SSE 与当前 GET 历史共 32 条，映射后的类型与数据逐项一致。事件构成为消息 6、用量 9、标题 1、计划 3、步骤 3、普通工具 8、等待 1、结束 1。普通工具对应写入、读取各一次及通知两次，提问经执行器转成消息与 wait，不能按普通 tool 数量覆盖所有工具动作。

写入调用 `call_12e236d521824d0db22f8298` 的事件 ID 为 `1789222490149-0` / `1789222490172-0`，领域创建时间相差 11.621 ms；读取调用 `call_c5b2fa88f7e149a4b5886d80` 为 `1789222526895-0` / `1789222526915-0`，相差 8.517 ms。各对映射后的秒级时间相同。这些间隔包含 calling 交接，不包括 called 后的预览同步与页面接收，不能作为纯工具或端到端性能结论。最终附件与字节验证沿用第九章记录。

9 条 UsageEvent 均 available，Planner 2 条、ReAct 7 条；单次 total 求和为 45,384，与会话累计一致；等待前后两轮分别 17,680 与 27,704。原时间窗口的会话日志匹配 9 条模型请求，标识为 `glm-5.2`。没有将当前配置当作历史完整请求快照；温度等逐次参数与完整出站正文未捕获。未新增真实模型任务，也不证明失败尝试、重复事件及收费类别全部被准确记录。运行容器的领域事件、接口映射、BaseAgent、模型适配和用量累计与当前本地文件逐字节一致。

已核对 HTML SSE 标准的事件分隔与 EOF 规则，以及 OpenTelemetry 的信号分类，来源链接维护在正文和素材索引。新增 `10-event-projection.svg`、`10-observation-intervals.svg`（1600×900、Fireworks Style 1），XML、marker、碰撞、几何、构图检查与 Chrome headless 渲染目视检查通过；通用 PNG 渲染依赖沿用此前缺失条件，临时预览未提交。未增加真实界面截图。正文链接、Python 简化片段语法及图文一致性已核对。

### 第 11 章沙箱与执行环境

正文、两张配图与观察脚本此前已完成。2026-09-14 按第 8–10 章同一节奏改叙事：开篇接事件与执行位置，补总表，把路径围栏、复用、隔离轴和回收收成「分」；TTL 三个环境变量名、已有沙箱 `create`/`get` 销毁差异、`RedisStreamTask.destroy` 遍历改字典收到本节与 map。本轮未重跑脚本、未改产品隔离实现、未重画 SVG。

2026-09-14 按第 8–10 章标准补齐欠账，只改 `11-sandbox-and-execution-environment.md`，未改产品代码、脚本与配图：

- 「动作在哪落地」与「相同路径为何找不到」各补一组通用选项对照（进程内执行 vs 独立环境；共享工作区 vs 每会话一份环境），与第八章、第九章的「常见策略」列写法一致。
- 「在容器里不等于隔离」由一段混写拆成四轴表与四段：文件系统（每容器可写层）、网络（共享 `SANDBOX_NETWORK`、无出口白名单）、身份（Supervisor `user=root`；`ShellExecuteRequest` 只有 `session_id`/`exec_dir`/`command`，无身份字段；Dockerfile 建 `ubuntu` 与免密 sudo 但默认未用；文件工具的 `sudo` 只作用于文件操作）、配额（创建参数无 CPU/内存/进程数上限）。
- 补软件条件一轴：Dockerfile 安装 Python 3.10、Node 24、Chromium、curl/wget 与包管理器并配置软件源，说明工具清单不是能力边界。
- 生命周期节补「随任务销毁 vs 会话级保留 + TTL」的取舍，以及 TTL 机制描述：`SupervisorService` 按 `server_timeout_minutes`（默认 60）设定倒计时并关闭 Supervisor，`auto_extend_timeout_middleware` 在每次 `/api/` 请求时延长 3 分钟（有请求即保活）。同时保留变量名不一致的结论：创建容器写入 `SERVICE_TIMEOUT_MINUTES`，沙箱读取 `server_timeout_minutes`（`SERVER_TIMEOUT_MINUTES` 前缀），二者无别名映射，因此「机制存在」不写成「本次部署已生效」。
- 「用观察结果判断边界」补七行情境表（同会话复用与跨会话分开、`..` 与符号链接、容器内身份、容器间接口请求、运行中配额、显式删除、任务结束后存留）与证据列；检查清单由 5 条扩为 6 条，加入软件条件与「读到 TTL 机制不等于已触发」。

上述新增内容以源码静态核对为主（`ray_agent/sandbox/{Dockerfile,supervisord.conf,app/core/config.py,app/core/middleware.py,app/services/supervisor.py,app/interfaces/schemas/shell.py}`、`ray_agent/api/app/infrastructure/external/sandbox/docker_sandbox.py`、`ray_agent/docker-compose.yml`），容器内身份与 TTL 计时状态已由下节实测补证。软件源、VNC/CDP 端点可达性、自动到期的完整回收过程、应用关闭清理、公网出口与租户隔离仍 `unverified`。

2026-09-14 补齐后按第 8–10 章标准复核，并对运行中的 Compose 与动态沙箱做只读实测，据此修正三处过时或笼统表述：

- 执行身份：经沙箱自身 shell 接口（产品路径）执行只读命令，得到 `uid=0`、`whoami=root`、`HOME=/home/ubuntu`。注意 `docker exec` 得到的 `HOME=/root` 不继承应用进程环境，不能代表产品执行路径，正文与本节不采用该值。
- TTL：`GET /api/supervisor/timeout-status` 返回 `active=true` 且剩余约 5,960 秒，说明倒计时在本次部署中已按沙箱自带默认 60 分钟启用，并由中间件按请求延长；被忽略的是产品侧 `SANDBOX_TTL_MINUTES`。正文原先「机制存在不等于这次部署已经生效」的框架与实测矛盾，已改为「机制已启用，但启用的是沙箱默认值；到期回收仍未观察到」。自动到期的完整观察仍 `unverified`。
- 已有地址模式销毁：`create()` 分支不保存容器名（`docker_sandbox.py:164-167`），`get()` 分支会传入 `container_name=id`（`:193-196`），`destroy()` 随即按名强制删除（`:180-182`）。正文原先的笼统表述只对 `create()` 成立，已就地限定。
- 顺带修正正文措辞：镜像安装的是 `chromium` 包；「显式删除动态容器」仅存在于适配层方法与验证脚本，产品没有对外删除接口。第 09 章补一句当前实现把事件、附件元数据与 Agent Memory 存为会话记录内的 JSONB 列，没有独立事件表；第 10 章事件清单补上 `title` 与 `error`。

同轮静态核对发现但**未修改**的产品问题：`prompts/system.py` 的 `<sandbox_environment>` 声称沙箱 `Node.js 20.18.0` 与「用户 `ubuntu`，拥有 sudo 权限」，实测为 Node v24.20.0、uid 0（root）。该提示经 Planner/ReAct 每次模型调用下发，无测试断言其内容；本节记录差异，是否修改产品提示另行确定。

既有本地脚本 `check_environment_boundaries.py` 与 Docker 脚本 `check_sandbox_environment.py` 仍适用。此前容器观察记录：按 ID 重连读到 `hello-ch11`，另一容器相同路径不存在；Shell `id -u` 为 `0`；同网络请求对端 `8080/api/supervisor/status` 为 HTTP 200；`Memory=0`、`NanoCpus=0`、`PidsLimit=null`；两个实验容器经 `destroy` 后查询不存在。API 创建写入 `SERVICE_TIMEOUT_MINUTES`，沙箱读取 `SERVER_TIMEOUT_MINUTES` / 字段 `server_timeout_minutes`，与 `SANDBOX_TTL_MINUTES` 无别名映射。自动 TTL 的完整回收、应用关闭清理、公网出口和租户隔离仍 `unverified`。

### 第 12 章文件与任务产物

2026-09-14，本轮以 `33f2ad8` 为代码基线，按「Harness 如何组织执行所需的文件，并把执行成果交给用户」重写正文。先解释文件用途、输入准备与工具观察，再讨论产物选择、保存访问、保留责任和交付验证；RayAgent 作为实现对照，开篇数据分析任务明确标为教学推演。版本行为只保留一个短例子，缺陷修复过程从正文移出，技术依据留在下方及素材索引。

本轮仅修改课程正文、配图和关联说明，没有修改产品代码、重新部署或运行模型任务。静态核对输入附件准备、工具结果进入 Memory、文件工具自动同步、消息附件同步和全空错误检查；链接与锚点检查通过。撤下原有两张存储/版本图，新增 `12-files-in-harness.svg`（1600×900、Fireworks Style 1），用执行循环与成果交付两个区域呈现通用职责。XML、marker、碰撞、几何和构图检查通过；检查脚本缺少内置渲染器，另用 Chrome headless 实际渲染并目视通过。临时 PNG 在工作区之外。理解检查改为解释职责、比较策略与设计验证，正文完成不等于故障实测全部通过。

下列实现修复及验证来自此前制作（初稿基线 `28e1bb8`、修复核对基线 `9053a56`），本轮保留证据，不重复运行。

**缺陷及最终修复：**

- 原 `_sync_file_to_storage` 已存在按路径查找、移除旧条目的逻辑，但调用 `remove_file` 时传的是路径，仓库实际按文件 ID 匹配，因而删除不生效。这是参数契约错误，不是保留版本的实现。审查时在独立 Python 命名空间加载 `git show HEAD:.../agent_task_runner.py` 的原始运行器，同路径替换用例失败，未修改或回退工作区文件。
- 最终改为传旧文件 ID，并先完成新副本上传，再在同一 UoW 事务里移除旧关联、添加新关联。`DBSessionRepository.remove_file` 显式 flush ORM 修改，适配生产 `autoflush=False`，避免后续 SQL 追加与未刷新的 ORM 值互相覆盖。上传失败保留旧条目；关联写入失败回滚列表，但已上传的副本不会由数据库事务删除。
- 修复只针对串行同步替换已有条目，不迁移历史重复数据，不保证并发或输入附件路径全局唯一，也不删除旧对象、改写历史附件或新增版本服务。

**既有实现验证（本轮未重跑）：**

- API 锁定环境 Python 3.12.13 / pytest 8.4.2，`tests/core/test_file_artifacts.py` 共 8 项通过：三次同路径同步、仓库按 ID 过滤、上传失败、关联失败、交付全部失败、交付部分失败、历史附件保留原 ID、历史重复数据不自动迁移。仓库/存储替身只验证确定性行为，内存事务快照不充当真实数据库回滚证据。
- `tests/core tests/protocols` 共 91 项通过，出现既有 Pydantic 弃用/序列化警告。本轮未重跑需要应用生命周期与外部服务的状态接口测试；初稿曾记录该入口在宿主机因 `manus-postgres` 不可解析而报环境错误。
- 新增 `scripts/check_file_artifacts.py`，使用真实 PostgreSQL、生产 `autoflush=False`、实际仓库/UoW 与临时本地存储。验证同事务替换只留新 ID、历史附件读 `v1` 而新 ID 读 `v2`、上传失败保留旧条目、关联写入异常时数据库回滚。全部数据库写入处于外层回滚事务，各 UoW 提交保存点；结束后查询确认专用会话记录不存在，临时文件清理完成。
- 本次数据库观察在运行中 API 容器的独立 Python 进程执行，从临时目录载入工作区修订后的运行器和仓库；未替换服务进程代码或重建部署。沙箱下载使用内存字节替身，不调用模型、Redis、真实沙箱或 COS；没有模拟网络丢失、数据库提交失败或进程崩溃。运行命令与条件维护在 [API 指南](../ray_agent/api/README.md#文件与产物观察)。

**此前真实任务证据（本轮未重跑）：**

初稿在重建 API 后经 nginx `localhost:8088` 发起同路径任务：`/home/ubuntu/ch12-version.txt` 先写 `v1`、覆盖 `v2`、读取并交付，会话 `fb1e059e-3e32-401a-a944-1534039260de`，记录耗时 36.0 秒、终态 `completed`。修复后会话当前 `files` 只留 1 条，该附件下载返回 200、2 字节 `v2`，沙箱读取也为 `v2`。修复前同类任务在同路径留下 4 条记录和 4 个存储 key；次数按全部同步计，不能解释为一次文件工具动作恰好同步四次。

当时快照记录文件表 33 行、磁盘 33 个对象；这不是本章任务独占的 33 个版本，也不是当前数量。当前列表只有一条不能推断会话历史只有一个引用，更不能证明所有旧对象都无引用。此前未删除沙箱后再下载，该现象在正文中仅按源码依赖解释。

**未覆盖：** 删除执行环境后下载、同名输入覆盖、完整故障页面表现、断连、大文件、并发同步、真实存储网络/提交故障和多用户访问范围仍 `unverified`；本轮未重跑第八章单进程脚本与第十一章边界脚本。副本清理、完整版本查询与部分附件失败补齐属于另行确定的产品范围。
