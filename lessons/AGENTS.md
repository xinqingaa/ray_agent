# 课程创作约定

本文件只维护 RayAgent 课程特有的稳定约定。编写或审查课程前，读取 [write-lessons](../skills/write-lessons/SKILL.md) 的通用写作方法，再按本章需要查看目录、素材与进度。

## 课程定位

以 RayAgent 为实践载体，学习 Agent Harness 的核心机制、系统设计与工程验证。Harness 指模型之外组织信息、行动、反馈与任务运行的工程职责组合，不指特定框架。

labs 用于隔离机制，RayAgent 用于研究机制组合后的设计与取舍。通用知识范围不受现有脚本限制，但应明确已有实现与能力差距。读者自行决定阅读、运行或修改，不设置必需、选做或进阶参与层级；课程不以从零搭建、TS 重写或框架迁移为目标。

## 内容归属

| 文件 | 唯一维护的内容 |
|---|---|
| [README](README.md) | 16 章阅读顺序、核心问题与章节分工 |
| 本文件 | 课程特有的稳定约定 |
| [根 PLAN](../PLAN.md) | 项目阶段与学习验收标准 |

运行条件与命令链接所属 lab 或服务指南；模块边界见 [架构说明](../docs/architecture.md)。通用写作方法只在 write-lessons 维护。

## 冻结约定

- 课程正文已冻结：默认不新增章节，不恢复制作账本或按章源码索引。
- 允许措辞、配图与局部勘误；机制表述与实现不一致时再改正文。
- 阅读路径为目录 → 本章 → 下一章；正文不链向已移除的制作文档。
- 缺口与 `unverified` 范围写在本章内；运行命令仍只维护在 lab 或服务指南。

## 写作约定

- 文件名保留英文，文档标题、小节标题、目录名称与正文使用中文。开头交代本章问题和阅读条件，章节导航集中在结尾。
- 每章先用任务现象与自然语言建立问题，按需用职责图说明关系；正文不以源码导览代替机制讲解。必要的短代码、标明简化的 Python/TS 模型及其解释遵循 write-lessons。
- 按证据与依赖安排示例，阅读顺序见 README。验证遵循[根工作指南](../AGENTS.md#文档约定)。
- 允许补充与课程直接相关的小范围 labs 脚本和 RayAgent 局部修复；新增产品功能、全局重构和框架迁移另行确定范围。

## 配图约定

默认交付 **SVG、1600×900、Fireworks Style 1（Flat Icon）**，放在 `lessons/assets/`，正文使用相对路径。只有用户明确指定时才改变对应格式、尺寸或风格；拥挤时精简或拆图。已有图片不因默认值变化批量重绘。

使用 Fireworks Tech Graph 时，读取已安装的 `fireworks-tech-graph` skill 和 `references/style-1-flat-icon.md`；不可用时如实说明。本课程参数优先于工具默认值：浅色背景，白色或浅色节点，4–12 px 圆角，3–6 px 节点边框与流程箭头；分组容器弱于主流程，少量强调色突出机制，不仅靠虚线区分层次。参数按实际渲染效果选择。

文字密度与术语选择遵循 [write-lessons](../skills/write-lessons/SKILL.md#draw-to-explain)。SVG 须做结构检查并实际渲染查看，临时预览不提交。技术示意图默认不交付 PNG、GIF 或 HTML；GIF 仅在明确指令下制作。用户授权的真实界面取证可保存为 PNG，按实际窗口尺寸保留必要区域，图注说明来源与观察含义，不套用示意图参数。
