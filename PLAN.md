# RayAgent 执行计划

## 目标与顺序

先系统理解 RayAgent 的核心逻辑和设计，再考虑定制功能。推进顺序为：

**运行现有项目 → 升级 A2A/MCP → 编写 lessons 并系统学习 → 二开 → 评估框架替换。**

学习完成意味着能够解释产品闭环、主要模块职责和关键设计，并能定位后续修改的入口。不要求先开发新功能，也不把全部代码逐行读完作为完成条件。

## 当前进度

| 阶段 | 状态 | 主要产出 |
|---|---|---|
| 1. 运行项目 | 已完成 | 2026-09-08 Compose 部署与内置文件工具任务已验证 |
| 2. 协议升级 | 已完成 | MCP 2.2、A2A 1.0 主产品与可复现 labs 基线 |
| 3. 系统学习 | 进行中 | 学习地图与 15 课骨架（Part A labs / Part B 产品）；正文与运行核对未开始 |
| 4. 二开 | 待学习完成 | 具体功能规格与实现 |
| 5. 框架评估 | 最低优先级 | 是否引入框架的判断依据 |

## 阶段 1：运行项目

首次运行使用现有依赖，集中解决环境、配置及阻塞运行的问题。

2026-09-08 本机已按 [运行指南](ray_agent/README.md) 完成 Compose 部署。会话 `3d78cb23-69d9-4751-b10c-f716be67d80f` 从 UI 发起「写入并读取 hello.txt」：Planner 拆成 2 步，ReAct 实际发出 `write_file` / `read_file`（`has_tool_calls=True`），本地文件存储同步成功，总结后任务正常结束，不再出现 SSE 收尾时的连接池 `CancelledError`。首次任务未启用外部 MCP/A2A。

- [x] 核对部署条件、依赖安装方式及服务连接配置。
- [x] 准备模型服务、数据库、Redis、沙箱和文件存储配置。
- [x] 启动应用，确认 UI、API 与沙箱之间能够连接。
- [x] 发起任务，验证规划、至少一次真实内置工具调用和最终结果。
- [x] 刷新页面，检查会话历史是否可查看。
- [x] 更新运行指南，记录可复现步骤及必要修复。

首次任务不依赖外部 MCP/A2A 服务。部署条件与验证步骤以 [运行指南](ray_agent/README.md) 为准。

**完成标准：** 从 UI 发起的任务完整执行并返回结果，历史可查看，启动步骤可复现。页面可见或健康检查通过只是中间检查。

## 阶段 2：升级 A2A/MCP

主产品 `ray_agent/` 与已确认范围内的 `labs/` 已完成升级和验收。

### 2A：主产品升级

- [x] 盘点主产品 MCP/A2A 实现、依赖及验证缺口。
- [x] 核对官方资料，制定版本目标与实施方案。
- [x] 用户确认主产品实施方案。
- [x] 升级 MCP，修复结果处理、工具路由、配置和资源生命周期。
- [x] 接入 A2A 官方 SDK，完成发现、调用、任务状态和错误适配。
- [x] 同步必要的设置页、事件与历史展示契约。
- [x] 同步依赖声明、锁文件和容器安装依赖文件。
- [x] 完成协议测试及从 UI 发起的 MCP/A2A 任务验证。
- [x] 在新会话中回归阶段 1 文件任务，验证重构后产生的历史及相关测试，更新运行与架构文档。

**主产品完成标准：** 只按目标新版实现 MCP/A2A，旧适配及兼容分支已移除；真实服务调用、错误状态、取消清理、重构后新会话历史和内置文件任务通过可复现验收；本地与容器依赖一致。旧格式历史回放不作为完成条件。保留自研 Plan + ReAct，不扩展全部协议可选功能。

### 2B：主产品完成后统一 labs

- [x] 根据主产品验收版本制定并确认“可复现核心基线”迁移范围。
- [x] 统一约定的实验依赖、示例与运行说明，并实际验证。

2A 与 2B 已分别验收。阶段 3 以 MCP 2.2、A2A 1.0 的主产品实现和已验证 labs 闭环为教学基准；外部凭据示例与旧综合参考不进入协议教学基线。

## 阶段 3：lessons 与系统学习

先划分主题、学习顺序和代码入口，再按顺序编写并校验教学文档。以阶段 2 验证后的实现为基准。

- [x] 建立课程章节、练习脚本与产品模块之间的学习地图。
- [ ] 按主题组织 lessons，并核对各练习的运行环境与资源路径。
- [ ] 按教学规范编写主题文档与理解检查。
- [ ] 通过运行、调试和观察验证文档内容。
- [ ] 补充状态归属、失败处理和已知限制。

主题地图、文档格式与教学要求统一维护在 [学习入口](lessons/README.md)。

本阶段以理解现有实现为目标，不要求新增产品功能。

**完成标准：**

- [ ] 能讲清 UI → 模型 → 工具与沙箱 → UI 的完整链路。
- [ ] 能说明主要模块职责、状态归属和数据存储位置。
- [ ] 能解释 Plan、ReAct、MCP、A2A 的分工及设计理由。
- [ ] 能定位常见运行问题，说明取消、迭代上限、断连和资源释放等行为与限制。
- [ ] 能指出新增工具或修改执行策略时需要阅读和调整的模块。

## 阶段 4：二开

学习完成后，根据实际需求选择第一项功能，写明问题、预期行为与验收条件，再实施修改。届时引入 `specs/` 和适当的 SDD 流程；需要模板工具时再创建 `.specify/`。

**完成标准：** 每项功能具有清楚的范围、实现和行为验证。

## 阶段 5：框架评估

理解现有自研 Loop 后，再评估 LangChain、LangGraph。优先通过独立实验比较状态管理、执行控制和维护成本，是否迁移由具体收益决定。

**完成标准：** 能说明框架解决什么问题、带来哪些成本，以及是否值得迁移。无需以引入框架作为学习成果。

## 下一步

阶段 3 骨架已按 01→15 落在 [学习入口](lessons/README.md) 与 [学习地图](lessons/map.md)。下一步先填 Part A（01–08，脚本），再填 Part B（09–15，产品）。产品观察保持 `unverified` 直到 Compose 复跑。
