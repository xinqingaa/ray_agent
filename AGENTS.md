# RayAgent 工作指南

## 项目定位

RayAgent 是 Agent Harness 教学与实验工程，以面向复杂任务执行的 AI Agent 应用为实践载体。课程学习 Harness 的核心机制、系统设计与工程验证。用户通过 Web 界面发起任务，系统以自研 Plan + ReAct 组织规划与工具调用，在沙箱中操作文件、终端和浏览器，并通过 MCP/A2A 接入外部工具与远程 Agent。

## 当前目标与 AI 工作角色

16 章旧版课程已完成验收，现作为演进基线。阶段 4 的核心重构与课程升级范围见 [PLAN.md](PLAN.md)（指向统一执行计划的软链接），工作包状态和验证证据只维护在[第四阶段进度](docs/research/phase-4-progress.md)。不能把目标方案写成当前已实现能力。

AI 按当前任务授权协助设计、实现、验证和课程更新；计划任务不自动授权产品实施。现有实现用于提供证据和案例，项目尚未具备的能力必须明确标注。

解释实现时区分代码事实、设计意图的推断和改进建议。产品改动按统一计划的工作包和验收范围推进；框架迁移、长期工作区等未纳入项不顺带实施。

## 按任务读取文档

| 任务 | 优先阅读 |
|---|---|
| 了解产品与文档入口 | [项目首页](README.md)、[文档地图](docs/README.md) |
| 理解职责分层与设计取舍 | [Harness 工程](docs/harness.md)、[设计取舍记录](docs/decisions.md) |
| 理解模块边界、执行流程和状态 | [架构说明](docs/architecture.md) |
| 确认某项能力是否已实现、证据如何 | [能力与边界](docs/capabilities.md) |
| 从文档或课程里的机制定位到代码 | [代码地图](docs/code-map.md) |
| 部署完整应用、检查服务连接 | [运行指南](ray_agent/README.md) |
| 启停 Compose、查看各服务日志 | [Docker 操作说明](ray_agent/DOCKER.md) |
| 修改后端、处理依赖和测试 | [API 开发指南](ray_agent/api/README.md) |
| 修改前端、处理请求与展示 | [UI 开发指南](ray_agent/ui/README.md) |
| 排查沙箱内执行与浏览器 | [沙箱开发指南](ray_agent/sandbox/README.md) |
| 运行实验或组织课程 | [实验入口](labs/README.md)、[教学规范](lessons/README.md) |
| 更新课程与配图 | [课程创作约定](lessons/AGENTS.md)、[旧方案背景](docs/background/README.md) |
| 确认阶段任务和完成标准 | [执行计划](PLAN.md) |

按当前任务选择相关文档，再定位代码；无需每次通读所有资料。

## 实现约定

- 沿现有分层定位修改，保持路由适配、应用协调、领域逻辑与基础设施访问的职责边界。
- 当前自研 Plan + ReAct 是重构基线；单循环与计划工具是阶段 4 目标。只有对应工作包的实施任务才替换主循环，协议接入或局部修复不顺带扩大范围。
- 修改工具、事件或持久化结构时，核对相关调用方、适配层和前端契约；具体边界见架构说明。
- 各服务及部分实验使用独立依赖环境，命令在所属指南指定的目录执行。
- 依赖升级同时核对本地锁定结果与容器安装来源，按服务指南验证。
- 保留必要的作者与许可证声明，配置和文档中不写入实际凭据。

## 文档规范

- 编写或审查教学内容遵循 [write-lessons](skills/write-lessons/SKILL.md)，技术文档需要配图时遵循 [draw-tech-diagrams](skills/draw-tech-diagrams/SKILL.md)；本节的 RayAgent 项目约定覆盖 skill 默认值。[课程创作约定](lessons/AGENTS.md) 只补充课程定位、过渡方式与修改范围。
- 项目文档使用中文；`lessons/` 的文件名保留英文，文档标题、小节标题、目录名称与正文使用中文。
- 资料按详细程度分层，不是按内容范围分层：`docs/` 概括、覆盖核心链路，`lessons/` 详细展开推导与备选方案，源码是最终事实、非必要不读。三层结论冲突时以源码为准；同一机制在 `docs/` 和 `lessons/` 各写一次不算重复维护，「同一项详细说明只维护一处」约束的是同一层内部。
- 课程章节开头交代本章问题和阅读条件，先用任务现象与自然语言建立问题，章节导航集中在结尾；正文不以源码导览代替机制讲解，必要的短代码与标明简化的 Python/TS 模型按 write-lessons 使用。
- 按证据与依赖安排课程示例，阅读顺序见[课程目录](lessons/README.md)。通用知识范围不受现有脚本限制，但必须区分通用策略、教学简化和项目已实现能力。
- 跨文档链接按需随文出现，不必集中在固定位置。代码位置集中写在[代码地图](docs/code-map.md)，用可点击的 Markdown 链接；其他文档提到代码不必重复贴路径。
- `docs/` 各文档的唯一职责见[文档地图](docs/README.md)；总体阶段与范围只维护在 PLAN 指向的统一计划，工作包状态与验证证据只维护在第四阶段进度，运行命令维护在所属 lab 或服务指南。
- 架构说明只在模块职责、主要执行路径、状态或资源所有权、跨服务通信方式变化时更新，不因局部字段或行为调整自动扩充。
- 能力边界、未验证范围和已知不一致统一写入[能力与边界](docs/capabilities.md)，标注证据类型与核对日期；其他文档正文陈述代码事实即可，不必逐句设防。
- 完整部署配置维护在应用运行指南，日常操作和本地开发命令维护在所属服务或 lab 指南；其他入口保留概览。
- 一次性修订意见与排障过程不追加为永久规范；调研文档保留日期和代码基线作为分析快照，引用时再核对适用性。

### 配图规范

本节完整定义 RayAgent 的配图偏好与存放规则；通用的选图、信息组织、格式取舍和验证方法见 [draw-tech-diagrams](skills/draw-tech-diagrams/SKILL.md)。

- 技术示意图默认交付 **SVG、1600×900、Fireworks Style 1（Flat Icon）**。采用浅色背景、白色或浅色节点、4–12 px 圆角、3–6 px 节点边框与流程箭头；分组容器弱于主流程，少量强调色突出机制。只有用户明确指定时才改变对应格式、尺寸或风格，已有图片不因默认值变化批量重绘。
- SVG 是技术示意图的唯一源文件和 Markdown 正式引用格式。PNG 只用于本地渲染预览、用户授权的真实界面取证或平台兼容副本；技术图不以截图代替结构表达。GIF 仅在明确要求动画时制作，普通文档不交付交互 HTML。
- `docs/assets/` 只存放跨章节的项目级概览图，`lessons/assets/` 只存放各章课程配图；正文使用相对路径。临时预览不提交。
- `docs/assets/` 维护四类项目级概览图，表达方式分别采用 draw-tech-diagrams 定义的能力地图、分层架构、状态/循环和知识路线：产品全景图讲产品能力与 Harness 职责，架构图讲当前组件边界、控制流和数据归属，任务运行图讲执行循环、反馈和结束路径，课程路线图讲知识阶段、核心问题和章节关系。每类只回答对应问题，不跨类混画，也不混入部署拓扑、用户体验、实现类名或未来方案；各章课程配图的选题不受此分类限制。
- 使用 Fireworks Tech Graph 时读取其 skill 和 `references/style-1-flat-icon.md`；本项目参数优先于工具默认值。交付前完成 draw-tech-diagrams 要求的结构检查、实际渲染、视觉检查与图文一致性核对。
- 用户授权的真实界面取证按实际窗口尺寸保留必要区域，图注说明来源与观察含义，不套用技术示意图的尺寸与风格参数。

## 验证要求

验证实际受影响的行为，并说明验证条件和结果。文档检查链接、代码依据与一致性；运行及协议修改使用相关服务指南中的检查入口。

明确区分静态核对与实际运行验证，未执行的命令不表述为已通过；已有检查的覆盖边界见各服务指南。
