# RayAgent 工作指南

## 项目定位

RayAgent 是 Agent Harness 教学与实验工程，以面向复杂任务执行的 AI Agent 应用为实践载体。课程学习 Harness 的核心机制、系统设计与工程验证。用户通过 Web 界面发起任务，系统以自研 Plan + ReAct 组织规划与工具调用，在沙箱中操作文件、终端和浏览器，并通过 MCP/A2A 接入外部工具与远程 Agent。

## 当前目标与 AI 工作角色

16 章课程已验收并冻结；当前处于阶段 4「评估与调研」，先判断要不要改、改哪一块，不预设产品改造开工。阶段与验收标准见 [PLAN.md](PLAN.md)。

AI 协助研究通用机制与设计取舍、阅读和讲解代码、排查运行问题、核对技术事实，以及在课程微调时核对正文与实现。现有实现用于提供证据和案例；项目尚未具备的能力必须明确标注。

解释实现时区分代码事实、设计意图的推断和改进建议。产品改动与框架迁移仅在阶段 4 评估后单独确定范围。

## 按任务读取文档

| 任务 | 优先阅读 |
|---|---|
| 了解产品与文档入口 | [项目首页](README.md)、[文档地图](docs/README.md) |
| 理解职责分层与设计取舍 | [Harness 工程](docs/harness.md)、[设计取舍记录](docs/decisions.md) |
| 理解模块边界、执行流程和状态 | [架构说明](docs/architecture.md) |
| 确认某项能力是否已实现、证据如何 | [能力与边界](docs/capabilities.md) |
| 从文档或课程里的机制定位到代码 | [代码地图](docs/code-map.md) |
| 部署完整应用、检查服务连接 | [运行指南](ray_agent/README.md) |
| 启停 Compose、查看各服务日志 | [Docker 操作说明](ray_agent/DOCKER.md) |
| 修改后端、处理依赖和测试 | [API 开发指南](ray_agent/api/README.md) |
| 修改前端、处理请求与展示 | [UI 开发指南](ray_agent/ui/README.md) |
| 排查沙箱内执行与浏览器 | [沙箱开发指南](ray_agent/sandbox/README.md) |
| 运行实验或组织课程 | [实验入口](labs/README.md)、[教学规范](lessons/README.md) |
| 微调已冻结课程 | [课程创作约定](lessons/AGENTS.md) |
| 确认阶段任务和完成标准 | [执行计划](PLAN.md) |

按当前任务选择相关文档，再定位代码；无需每次通读所有资料。

## 实现约定

- 沿现有分层定位修改，保持路由适配、应用协调、领域逻辑与基础设施访问的职责边界。
- 自研 Plan + ReAct 是核心实现。协议接入或局部修复不顺带替换主循环。
- 修改工具、事件或持久化结构时，核对相关调用方、适配层和前端契约；具体边界见架构说明。
- 各服务及部分实验使用独立依赖环境，命令在所属指南指定的目录执行。
- 依赖升级同时核对本地锁定结果与容器安装来源，按服务指南验证。
- 保留必要的作者与许可证声明，配置和文档中不写入实际凭据。

## 文档约定

### 资料分层与深度递进

资料按深度分三层，读者按需下探，不要求逐层读完：

| 层 | 位置 | 回答 | 写法 |
|---|---|---|---|
| 扫描层 | `docs/` | 是什么、为什么这么选、做到了什么程度 | 结论先行，供人和 Agent 快速建立认识 |
| 推导层 | `lessons/` | 怎么推导出来、有哪些备选、为什么排除 | 教学展开，正文已冻结 |
| 事实层 | 源码与测试 | 到底怎么实现的 | 以代码为准 |

同一机制允许在扫描层和推导层各写一次，粒度不同，这不算重复维护；「同一项详细说明只维护一处」约束的是同一层之内。三层结论冲突时以源码为准，其次扫描层；课程正文不因 `docs/` 变化回改。

### 引用形式

- 正文段落内不挂跨文档链接。导航只出现在两处：文首一句说明本文维护什么，文末一处「继续深入」给出向下的出口。
- 出口指向推导层的章节或[代码地图](docs/code-map.md)的对应行，表达深度递进，不做同层之间的横向跳转。
- 代码位置一律用反引号写裸路径，不用 Markdown 链接。路径重构后链接会静默失效，裸路径不会。
- 机制到代码位置的成组映射只维护在[代码地图](docs/code-map.md)；各服务 README 的「代码导航」按目录职责组织，供服务内开发使用。两者索引方式不同，不互相合并。

### 内容归属

- 项目文档使用中文；`lessons/` 的文件名保留英文，文档标题、小节标题与正文使用中文。编写、审查或更新教学内容前，读取并遵循 [课程创作约定](lessons/AGENTS.md) 及其引用的通用写作 skill。
- 同一层内同一项详细说明只维护一处，其他文档保留简短概览；`docs/` 的读者分流与各文档的唯一职责见[文档地图](docs/README.md)。项目阶段状态只维护在 PLAN，运行命令维护在所属 lab 或服务指南。
- 架构说明只在模块职责、主要执行路径、状态或资源所有权、跨服务通信方式变化时更新。字段增减、内部函数调整、局部错误处理和页面行为修改，只更新直接受影响的说明与测试，不因“契约变化”自动扩充架构文档。
- 能力边界、未验证范围和已知不一致统一写入[能力与边界](docs/capabilities.md)，并标注证据类型与核对日期。其他文档正文只陈述代码事实，不在段中逐句设防；“但是、不保证、未验证”这类限定不散写在正文里。
- 完整部署配置维护在应用运行指南，日常 Compose 操作维护在 Docker 说明，本地开发与测试维护在所属服务或 lab 指南；其他入口只保留概览。
- 一次性修订意见与排障过程不追加为永久规范；调研文档保留日期和代码基线，作为当时的分析快照，引用时再核对适用性。

### 配图规范

- 非课程配图使用纯 SVG；产品介绍图不嵌入真实界面截图或位图，技术图不以截图代替结构表达。PNG 仅可作为本地渲染预览，不作为 Markdown 正式引用或仓库交付物。
- 一张图只回答一个主要问题。产品全景图讲产品能力与 Harness 职责；架构图讲当前组件边界、控制流和数据归属；任务运行图讲执行循环、反馈和结束路径；课程路线图讲知识阶段、核心问题和章节关系。不要把部署拓扑、用户体验、实现类名和未来方案混在同一张图里。
- 先确定读者需要理解的关系，再选择能力地图、分层架构、状态/循环或知识路线布局。节点使用短标签，详细解释写在正文或图注；用有语义的连接线表达调用、反馈、保存、交付和续接，不使用装饰性箭头。
- 技术图应统一字体、网格、字号层级和颜色语义，同时根据内容调整比例；信息拥挤时精简或拆图，不靠缩小文字容纳段落。课程总览图放在 `docs/assets/`，`lessons/assets/` 只放各章课程配图。
- 使用 `fireworks-tech-graph` 或等效 SVG 工具制作时，SVG 是唯一源文件。交付前运行结构、标记、几何和构图检查，并在实际 Markdown 阅读宽度下渲染查看文字、连线、留白、裁切和中文字体；检查未执行或失败时如实说明。

## 验证要求

验证实际受影响的行为，并说明验证条件和结果。文档检查链接、代码依据与一致性；运行及协议修改使用相关服务指南中的检查入口。

明确区分静态核对与实际运行验证，未执行的命令不表述为已通过；已有检查的覆盖边界见各服务指南。
