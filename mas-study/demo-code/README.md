# Demo-Code

## 安装&同步环境命令

在安装了 uv 的前提下，在当前目录使用如下命令，会自动安装对应的 Python 3.12 环境并同步安装依赖信息：

```bash
uv sync
```

亦或者在安装了 pip 的前提下，可以使用 requirements.txt 安装对应的依赖，命令如下：

```bash
pip install -r requirements.txt
```