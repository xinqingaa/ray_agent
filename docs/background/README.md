# 重构背景：从 Plan + ReAct 到受控的单循环 Harness

整理日期：2026-09-19。旧实现与课程基线：`2c323ccffdf6f58ad1e8b1214c0a12bb6b8c1a45`。本目录说明演进原因、旧方案的价值与边界、历史证据，不维护另一套当前架构或旧课程。

新版目标及实施顺序见[统一执行计划](../research/phase-4-plan.md)，实际完成情况见[进度](../research/phase-4-progress.md)。整理时产品尚未重构，当前 docs 与课程正文仍描述旧实现；后续对应工作包通过后更新为新版。这里的“旧”是相对目标设计而言，不表示当前代码已经被替换。

## 旧方案解决了什么

RayAgent 原方案是 Web UI、API、沙箱、PostgreSQL、Redis 和文件存储组成的任务执行系统。外层 Plan + ReAct Flow 创建计划、选择步骤、更新剩余计划、总结；内层 Agent 请求模型、执行工具、回填结果。Planner 与执行器各自保存消息记忆。

文件、Shell 和浏览器在沙箱中执行；MCP 与 A2A 接入工具层；业务事件经 SSE 展示并保存；产物同步为可下载附件。会话可多轮，沙箱按会话复用。它已经展示了一个 Harness 的核心职责和跨模块交接，不是因为“不像 Codex”就失去价值。

旧方案的教学价值在于：能明确观察步骤与工具反馈两种边界、模型输出与真实动作的区别、协程/持久记录/外部副作用的不同寿命，以及协议适配如何接入自研循环。原 16 章已完成系统学习任务，阶段 3 的验收仍然有效。

## 为什么升级

| 旧选择及原收益 | 暴露的代价 | 新方案的演进方向 |
|---|---|---|
| 独立 Planner 和步骤调度，方便展示进度 | 强制交接、两份上下文、步骤 JSON 和重复规划；开放任务可能一步做完多步 | 单循环推进，计划作为工具状态；程序保留控制边界 |
| 保存消息列表，易于多轮交互 | 工作上下文与持久事实混合，定点删正文不能处理长轮次 | 原始记录、摘要和请求 Context 分开，主动预算 |
| 模型整包返回，解析简单 | 生成期间无增量内容 | 保留 SSE，增加模型内容与工具参数的流式契约 |
| 会话状态同时表示运行结果 | 停止和完成混合，重启后难解释执行情况 | 独立 Run/调用身份与中断、取消、等待语义 |
| 协程取消与通用重试，实现直接 | OS 进程可能继续，写操作响应丢失后可能重复 | 资源所有权、取消确认、副作用分类与未知结果对账 |
| 总结与假完成守卫 | 有调用不等于产物正确、上传成功 | 具体产物校验，执行结束与成果验证分开 |

数据库本身不是问题：Web 服务继续使用 PostgreSQL 合理。JSONL 更适合某些本地追加日志场景，不能自动解决上下文、恢复和并发。worktree 分离文件修改，不自动隔离记忆、权限或网络。

## 历史资料合并与证据来源

原 research 实际为 5 篇正文。它们的结论已合并到统一计划，历史事实集中于本文；不再保留互相竞争的候选路线。原先已提交的三篇全文和旧课程/配图可在上述 Git 基线中追溯；本轮两篇讨论稿的有效结论与修正已纳入新计划，不继续维护独立稿。

| 原资料 | 日期与基线 | 保留的结论及适用范围 |
|---|---|---|
| 工作区与 Harness 演进调研 | 2026-09-09；RayAgent `a798654`、learn-codex `bebbd85` | 原产品值得渐进改造，不需先换框架/语言；项目、目录、会话、运行与环境寿命不同；工作区现已明确后置 |
| Harness 工程课程评审 | 2026-09-12；RayAgent `6422e9f`、learn-codex `fed06fa` | 16 章知识结构可保留；步骤边界不等于授权；增加复杂度要有可观察收益；许多课程修订随后已落实 |
| Harness 能力评估 | 2026-09-14；RayAgent `28e1bb8` | 有真实文件任务、沙箱路径观察；发现 Context、流式、状态和交付问题；不是当前缺陷的未经复核清单 |
| RayAgent 与 Codex 对比 | 2026-09-19；RayAgent `2c323cc`、learn-codex `59bcb8b` | 区分循环选型、日志/记忆、工作区和流式；保留技术栈并聚焦核心增强 |
| 改造范围复审 | 2026-09-19；新增 Codex Rust `78245b47` | 定向核对真实源码；撤回固定 12×3 对比门槛；提前决定循环；成果验证与课程更新纳入范围 |

### 工作区调研的本地观察

2026-09-09 在 macOS 临时目录调用实际 ShellService 执行 `sleep 6`，约 6.02 秒后返回 completed，而非预期约 5 秒返回 running。观察指向输出读取器被直接等待的问题。未经过 Docker/HTTP，不能把这个结果当所有平台的现行结论；计划 P3 要复核并验证句柄与进程管理。

### 能力评估的真实任务记录

2026-09-14 本机 Compose，经 `localhost:8088`，模型 `glm-5.2`：创建文件写入 `harness-eval`，读回并交付附件。会话 `3cb937b6-6b95-46c5-94af-16a14e16cfc0`，旧报告记录终态 completed。

| 原观察 | 保留与解释 |
|---|---|
| 约 36.4 秒、13 次模型调用（规划 3、执行 10） | 单次旧基线任务，不是总体性能或新方案预期 |
| 累计 prompt tokens 64,763，completion tokens 1,295 | 输入重复发送的累计用量，不是唯一上下文大小，更不是费用 |
| 7 次工具调用：3 次文件动作、4 次通知 | 可作为冗余交互的线索，不能证明所有双循环都如此 |
| 一次文件写入、4 条同路径记录、1 个交付附件 | 当时的 id/path 契约缺陷；当前基线调用方已按旧文件 ID 删除，不能再排作未修缺陷 |
| 任务结束后容器仍运行 | 会话复用设计，不等于资源自动泄漏已被证明 |
| 提示描述 ubuntu，路径探针显示 uid=0；Node 为 24.x | 以产品执行路径而非宿主 docker exec 环境为证据，权限/环境需在新镜像重新核对 |
| “约 14 秒首次输出” | 原报告括注起止时刻相差约 34 秒，记录自相矛盾；不作为重构前性能基准 |

原记录未覆盖 SSE 断连、数据库提交故障、API 崩溃接管、多 worker、TTL 自动回收、公网策略、完整浏览器端到端等范围。本文是对历史报告的整理，没有重新执行这些任务，也未重新取得原完整日志。

### 当前基线已修正与仍需处理的边界

当前静态核对：文件替换使用旧文件 ID；未知工具分支使用 raise；A2A 取消时已有远端取消尝试。不得把旧报告的问题原样搬成重构待办。当前仍有取消写 completed、单步预算、消息局部裁剪、非流式模型、只取首个工具调用、进程内运行注册表等结构，见当前能力与代码地图。

Codex 的真实源码证据及固定提交链接统一在执行计划中；教学拆解仅作理解线索。未运行 Codex，因此不宣称其所有恢复、权限和外部副作用路径都得到验证。

## 课程和图怎样过渡

旧课程不是废弃材料，它完成了上一阶段的机制学习，并提供原方案的推导和观察背景。新阶段保持“问题 → 通用机制 → 新实现 → 验证”的课程结构；旧方案的具体流程与旧运行证据不继续占据课程主线。

当前课程尚未更新时，由课程入口说明基线。每个实现包验收后，对应正文及图同步升级；最后整体审查。通用双循环对比可以保留为备选方案，不再称为 RayAgent 当前主循环。旧图和截图从新版主线撤下前先确认引用和 Git 历史，不整套复制到背景目录。

本目录只维护演进背景。当前架构、能力和代码入口仍在 docs；详细新机制在 lessons；未来工作和验收在 research 的统一计划；完成证据在独立进度文档。
