# RayAgent

RayAgent 是一个围绕 **Agent Harness 工程**构建的实践项目：一套可运行的任务执行系统，加上一整套把它拆开讲清楚的工程文档与课程。

![RayAgent 产品全景：上下文与记忆、控制与观察、工具与环境、外部协作围绕 Plan + ReAct 执行核心](docs/assets/product-overview.svg)

## 这个项目在解决什么工程问题

模型一次调用只能做一件事：读入一段文本，输出一段文本或一次工具调用请求。它不会真的打开文件，不知道上一步做过什么，也不会在失败后重来。要让它完成一项真实任务，缺的不是更强的模型，而是模型之外的一整套运行系统——准备上下文、组织计划、执行动作、接住反馈、保存状态、交付成果，并且在中途可以被观察和打断。这套职责组合就是 **Harness**。

Harness 工程的难点不在于让循环跑起来，而在于把几件长得很像的事分开：模型说"文件已写入"和文件真的存在，是两件事；页面显示任务结束和目标已经达成，是两件事；历史能完整回放和执行能从中断处继续，也是两件事。分不开，系统就会在看起来正常的时候给出错误结论。

RayAgent 把这些职责拆成五层实现——会话与任务编排、规划层、决策层、工具契约层、执行适配层——并让观测控制与状态持久化贯穿其中。每一层的边界、失败方式和设计代价都写成了可核对的文档，而不是留在代码里让人自己推断。

这个项目不回避没做到的部分。流式输出、并行工具调用、上下文预算、幂等与执行恢复都还没有；哪些路径经过实际运行验证、哪些只做了静态核对，全部按证据类型列在同一张清单里。

## 核心设计

执行核心是自研的 **Plan + ReAct** 双层循环：外层由程序按步骤状态调度并在步骤边界更新计划，内层由模型在步骤内自由调用工具直到收口，两层只在步骤边界交接。不使用现成 Agent 框架，因为框架会把上下文装配、循环退出、失败处理这些最值得研究的判断封装成配置项。

动作发生在隔离的沙箱环境里：文件与 Shell 走沙箱服务，浏览器通过 CDP 连接沙箱内的 Chromium。外部能力通过 MCP 接入工具、通过 A2A 委派远程 Agent，两者都接进同一个工具契约层，主执行流程不变。

## 能做什么

| 能力 | 在产品中如何体现 |
|---|---|
| 规划与执行 | 提交目标后自动生成计划、逐步执行，并按步骤结果调整剩余安排 |
| 工具与环境 | 在沙箱中读写查找文件、执行命令与管理进程、检索网页、操作浏览器 |
| 控制与观察 | 实时查看计划、步骤与工具记录；Agent 可提问，用户可补充要求或请求停止 |
| 外部协作 | 通过 MCP 调用外部服务提供的工具，通过 A2A 把任务委派给远程 Agent |
| 文件与交付 | 上传资料同步进执行环境，产出文件同步为消息附件，可预览和下载 |

## 从哪里开始

资料按深度分三层：`docs/` 给结论，`lessons/` 给推导，代码是事实。按需下探，不必逐层读完。

**想快速认识这个项目**，包括让其他 Agent 建立认识——先看 [Harness 工程](docs/harness.md)，它是理解整个项目的主入口，职责如何分层、每层的失败方式和设计代价都在里面；再看[能力与边界](docs/capabilities.md)确认每项能力的实现程度和证据类型。关键选型的备选方案见[设计取舍记录](docs/decisions.md)，用户在界面上能做什么见[产品说明](docs/product.md)。

**想系统学习 Agent Harness 工程**——从[课程](lessons/README.md)开始。16 章按六个阶段组织，从最小行动闭环讲到可靠性验证，每章配可运行的[实验](labs/README.md)，并对照本项目的实现说明设计取舍。这是深度最高的一层，值得按顺序读完。

**想改代码**——用[代码地图](docs/code-map.md)从机制定位到文件和回归测试，用[架构说明](docs/architecture.md)确认模块边界、状态归属与控制参数，再按[运行指南](ray_agent/README.md)部署，各服务的开发命令在各自的 README 里。

文档之间的分层与更新条件见[文档地图](docs/README.md)，项目阶段与验收标准见[执行计划](PLAN.md)。
