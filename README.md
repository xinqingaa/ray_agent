# RayAgent

RayAgent 是一个围绕 **Agent Harness 工程**构建的实践项目：一套可运行的任务执行系统，加上一整套把它拆开讲清楚的工程文档与课程。

![RayAgent 产品全景：上下文与记忆、控制与观察、工具与环境、外部协作围绕 Plan + ReAct 执行核心](docs/assets/product-overview.svg)

## 为什么需要 Harness

模型一次调用只能做一件事：读入一段文本，输出一段文本或一次工具调用请求。它不会真的打开文件，不知道上一步做过什么，也不会在失败后重来。把这一次判断变成一项能持续推进、能被打断、能交付成果的任务，靠的不是更强的模型，而是模型之外的一整套运行系统——组织上下文、分发和校验工具调用、发布可观察的事件、把执行状态存下来、判断任务该继续还是收尾。这套职责就是 **Harness**。

难点不在于让循环跑起来，而在于把几件长得很像的事分开：模型说"文件已写入"和文件真的存在，是两件事；页面显示任务结束和目标已经达成，是两件事；历史能完整回放和执行能从中断处继续，也是两件事。分不开，系统就会在看起来正常的时候给出错误结论。

RayAgent 用一个自研的 **Plan + ReAct** 双层循环组织这些职责：外层按步骤状态调度、在步骤边界更新计划，内层由模型在步骤内自由调用工具直到收口，两层只在这个边界交接。围绕这个循环继续向下拆成五层——会话与任务编排、规划、决策、工具契约、执行适配；工具动作发生在隔离的沙箱里，外部能力通过 MCP 和 A2A 接入同一个执行核心，不单独另起一条流程。观测控制与状态持久化贯穿所有层，用来在"模型说完成了"和"目标真的达成"之间划出一条可核对的界线。

每一层的边界、失败方式和设计代价都写成了文档；还没做到的部分见[能力与边界](docs/capabilities.md)。

## 核心设计

不使用现成 Agent 框架，是因为框架会把上下文装配、循环退出、失败处理这些最值得研究的判断封装成配置项，自研版本才能逐行追踪一次任务里信息与控制的交接。

动作发生在隔离的沙箱环境里：文件与 Shell 走沙箱服务，浏览器通过 CDP 连接沙箱内的 Chromium。

## 能做什么

| 能力 | 在产品中如何体现 |
|---|---|
| 规划与执行 | 提交目标后自动生成计划、逐步执行，并按步骤结果调整剩余安排 |
| 工具与环境 | 在沙箱中读写查找文件、执行命令与管理进程、检索网页、操作浏览器 |
| 控制与观察 | 实时查看计划、步骤与工具记录；Agent 可提问，用户可补充要求或请求停止 |
| 外部协作 | 通过 MCP 调用外部服务提供的工具，通过 A2A 把任务委派给远程 Agent |
| 文件与交付 | 上传资料同步进执行环境，产出文件同步为消息附件，可预览和下载 |

## 从哪里开始

`docs/` 概括、覆盖核心链路，`lessons/` 详细展开推导过程和备选方案，代码是最终事实。按需选择详细程度，不必逐层读完。

**想快速认识这个项目**，包括让其他 Agent 建立认识——先看 [Harness 工程](docs/harness.md)，它是理解整个项目的主入口，职责如何分层、每层的失败方式和设计代价都在里面；再看[能力与边界](docs/capabilities.md)确认每项能力的实现程度和证据类型。关键选型的备选方案见[设计取舍记录](docs/decisions.md)，用户在界面上能做什么见[产品说明](docs/product.md)。

**想系统学习 Agent Harness 工程**——从[课程](lessons/README.md)开始。16 章按六个阶段组织，从最小行动闭环讲到可靠性验证，每章配可运行的[实验](labs/README.md)，并对照本项目的实现说明设计取舍。

**想改代码**——用[代码地图](docs/code-map.md)从机制定位到文件和回归测试，用[架构说明](docs/architecture.md)确认模块边界、状态归属与控制参数，再按[运行指南](ray_agent/README.md)部署，各服务的开发命令在各自的 README 里。

文档之间的分层与更新条件见[文档地图](docs/README.md)，项目阶段与验收标准见[执行计划](PLAN.md)。
